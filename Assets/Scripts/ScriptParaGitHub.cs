using UnityEngine;

public class ScriptParaGitHub : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    //COMENTADO PAU
    void Start()
    {
        //Codigo escrito por Javi hola
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
